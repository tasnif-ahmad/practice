let isLoggedIn = true;  // Boolean value

if (isLoggedIn) {
    document.getElementById("output").innerHTML = "Welcome back, user!";
} else {
    document.getElementById("output").innerHTML = "Please log in first.";
}
